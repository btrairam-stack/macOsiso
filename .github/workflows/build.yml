name: Build macOS ISO

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 60
    steps:
      - name: Download Full Catalina Installer
        run: |
          curl -L "https://swcdn.apple.com/content/downloads/26/37/001-68446/r1dbqtmf3mtpikjnd04cq31p4jk91dceh8/InstallAssistant.pkg" -o ~/InstallAssistant.pkg

      - name: Install Catalina App
        run: |
          sudo installer -pkg ~/InstallAssistant.pkg -target /
          ls /Applications/ | grep -i install

      - name: Create ISO
        run: |
          hdiutil create -o /tmp/Catalina -size 8500m -volname "Install macOS Catalina" -layout SPUD -fs HFS+J
          hdiutil attach /tmp/Catalina.dmg -noverify -mountpoint /Volumes/Catalina
          sudo /Applications/Install\ macOS\ Catalina.app/Contents/Resources/createinstallmedia --volume /Volumes/Catalina --nointeraction
          hdiutil detach /Volumes/"Install macOS Catalina"
          hdiutil convert /tmp/Catalina.dmg -format UDTO -o /tmp/CatalinaFinal
          mv /tmp/CatalinaFinal.cdr /tmp/Catalina.iso

      - name: Upload ISO
        uses: actions/upload-artifact@v4
        with:
          name: Catalina-ISO
          path: /tmp/Catalina.iso
